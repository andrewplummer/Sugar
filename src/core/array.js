import Sugar, { createNamespace } from '../core';

createNamespace('Array');

export const {
  defineStatic,
  defineInstance,
  defineStaticAlias,
  defineInstanceAlias,
} = Sugar.Array;

export default Sugar.Array;
